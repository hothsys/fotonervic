# Python dependencies
#
# Required
Pillow>=12.3.0

# Optional but strongly recommended
# Enables full RAW support: NEF, ARW, CR2, CR3, DNG, RAF, RW2, ORF, PEF, SRW
rawpy>=0.27.0

# Optional — enables HEIC/HEIF support (iPhone photos)
pillow-heif>=1.4.0
